/**
 * Handles imports in a different file so the JVM doesn't freak out if a mod isn't present at runtime, even though its logic will never fire when it doesn't exist.
 */
@MethodsReturnNonnullByDefault
package btpos.dj2addons.common.modrefs;

import mcp.MethodsReturnNonnullByDefault;